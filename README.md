I am building this project to sharpen my skills and have a proper backend project.
I will build this in multiple phases because I don't have the luxury of a full day to build this. I am a working.Net Enginner.

Phase 1 ( Completed )
1. ICommand Interface - all the commands will implement this to make sure there is consistency.
2. RedisStore.cs, where my commands exists and we perform the get, set, and other queries.
3. CommandHandler.cs, The first step, where we clean and process the raw user input and break it into a format that our commands can operate on.
